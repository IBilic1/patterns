export type ContentProp = {
    size: number,
    content: string,
    dateTime: string,
    format: string
}

export type ContentProp_ = {
    image: string,
}

export type ContentApiProp = {
    id:number,
    size: number,
    content: string,
    dateTime: string,
    format: string
}